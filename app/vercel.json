{
  "buildCommand": "npx expo export --platform web && node scripts/build-web.mjs",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/" }]
}
